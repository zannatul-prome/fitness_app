name: fitness_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.20.0
  firebase_auth: ^4.4.0
  shared_preferences: ^2.1.0
  intl: ^0.18.1
  flutter_local_notifications: ^13.0.0
  # pedometer: ^2.1.0     # Enable this later for real step counter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
